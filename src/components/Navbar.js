import React from 'react';

const Navbar = () => {
    return (
        <div className="navbar bg-purple-700 text-center">
            <a className="btn btn-ghost normal-case text-white text-xl ">Expense Tracker</a>
        </div>
    );
};

export default Navbar;